# 🧾 Simple Expense Tracker (Python + Tkinter)

A basic desktop **Expense Tracker** built using **Python**, **Tkinter**, and **CSV**.  
It allows users to add, view, and calculate total expenses with a simple GUI.

---

## 🚀 Features

- Add new expenses with **Date**, **Category**, **Amount**, and **Note**
- View all saved expenses in a popup window
- Automatically saves data to a **CSV file (`expenses.csv`)**
- View total expenses instantly
- Simple and clean **Tkinter interface**

---

## 🧰 Technologies Used

- **Python 3**
- **Tkinter** – GUI interface
- **CSV** – data storage
- **OS** – file handling
